# Spatial Command
Translates agricultural map intents (`fly_to`, `orbit`, `outline`, layer toggles, nearby search, distance, route animation, terrain and temporal replay) into typed map actions. The schemas live in `packages/shared/src/tool-schemas.ts`.

